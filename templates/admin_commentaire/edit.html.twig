{% extends 'base.html.twig' %}

{% block title %}Modifier un avis{% endblock %}

{% block body %}
    <div class="container py-4">
        <div class="d-flex flex-wrap align-items-center justify-content-between gap-3 mb-4">
            <h1 class="h3 mb-0">Modifier un avis</h1>
            <a class="btn btn-outline-secondary" href="{{ path('app_admin_commentaire_index') }}">Retour</a>
        </div>

        {{ include('admin_commentaire/_form.html.twig', { button_label: 'Mettre a jour' }) }}

        <div class="mt-3">
            {{ include('admin_commentaire/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
