{# templates/reset_password/check_email.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}E-mail de réinitialisation envoyé{% endblock %}

{% block body %}
<div class="min-vh-100 d-flex align-items-center bg-light py-5">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-12 col-sm-10 col-md-8 col-lg-6 col-xl-5">

        <div class="card shadow-sm border-0">
          <div class="card-body p-4 p-md-5 text-center">
            
            <div class="rounded-circle d-inline-flex align-items-center justify-content-center bg-success bg-opacity-10 p-3 mb-3">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor" class="text-success">
                <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
              </svg>
            </div>

            <h1 class="h4 mb-3">E-mail de réinitialisation envoyé</h1>

            <p class="text-muted mb-3">
              Si un compte correspondant à votre adresse e-mail existe, un message vient de vous être envoyé.
              Il contient un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
            </p>

            <p class="small text-muted">
              Ce lien expirera dans 
              <strong>
                {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}
              </strong>.
            </p>

            <p class="mt-4">
              Si vous ne recevez pas d’e-mail, vérifiez votre dossier de courriers indésirables
              ou <a href="{{ path('app_forgot_password_request') }}" class="text-decoration-none">essayez à nouveau</a>.
            </p>

            <div class="mt-4">
              <a href="{{ path('app_login') }}" class="btn btn-primary w-100">Retour à la connexion</a>
            </div>
          </div>
        </div>

        <p class="text-center text-muted small mt-3 mb-0">
          Pensez à vérifier votre boîte de réception dans les prochaines minutes.
        </p>

      </div>
    </div>
  </div>
</div>
{% endblock %}
